{
  "name": "aqua0-ethglobal",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua0 ETHGlobal Continuity Graph MCP workspace",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "check-env": "node scripts/check-env.mjs",
    "clean": "pnpm -r clean",
    "graph:build": "pnpm --filter @aqua0/subgraph graph:build",
    "graph:codegen": "pnpm --filter @aqua0/subgraph graph:codegen",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
